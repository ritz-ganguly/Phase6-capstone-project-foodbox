import { CustomerFilterByCuisinePipe } from './customer-filter-by-cuisine.pipe';

describe('CustomerFilterByCuisinePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerFilterByCuisinePipe();
    expect(pipe).toBeTruthy();
  });
});
